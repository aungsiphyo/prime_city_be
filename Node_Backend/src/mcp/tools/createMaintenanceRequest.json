{
  "name": "createMaintenanceRequest",
  "description": "Create a maintenance request for the resident",
  "parameters": {
    "type": "object",
    "properties": {
      "title": { "type": "string" },
      "description": { "type": "string" },
      "hasUsefulDetail": { "type": "boolean" }
    },
    "required": ["title", "description"]
  }
}
