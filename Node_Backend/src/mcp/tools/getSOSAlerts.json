{
  "name": "getSOSAlerts",
  "description": "Retrieve recent SOS alerts",
  "parameters": {
    "type": "object",
    "properties": {
      "limit": {
        "type": "number",
        "description": "Maximum number of SOS alerts to return"
      },
      "status": {
        "type": "string",
        "description": "Filter SOS alerts by status"
      }
    }
  }
}
