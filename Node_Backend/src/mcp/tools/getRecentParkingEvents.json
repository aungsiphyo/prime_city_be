{
  "name": "getRecentParkingEvents",
  "description": "Get latest parking change events from the database",
  "parameters": {
    "type": "object",
    "properties": {
      "limit": {
        "type": "number",
        "description": "Maximum number of parking events to return"
      },
      "type": {
        "type": "string",
        "enum": ["visitor", "resident"],
        "description": "Filter parking events by parking type"
      }
    }
  }
}
